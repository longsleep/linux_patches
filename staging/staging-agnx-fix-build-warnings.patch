From b92b68b5a5632a1583d4baf76d09fb95cf35b47f Mon Sep 17 00:00:00 2001
From: Greg Kroah-Hartman <gregkh@suse.de>
Date: Thu, 13 Nov 2008 15:34:13 -0800
Subject: Staging: agnx: fix build warnings

Fix some minor build warnins in the agnx driver

Cc: Li YanBo <dreamfly281@gmail.com>
Signed-off-by: Greg Kroah-Hartman <gregkh@suse.de>
---
 drivers/staging/agnx/pci.c |    6 +++---
 drivers/staging/agnx/phy.c |    2 ++
 2 files changed, 5 insertions(+), 3 deletions(-)

--- a/drivers/staging/agnx/pci.c
+++ b/drivers/staging/agnx/pci.c
@@ -207,7 +207,7 @@ static void rings_free(struct agnx_priv 
 	spin_unlock_irqrestore(&priv->lock, flags);
 }
 
-
+#if 0
 static void agnx_periodic_work_handler(struct work_struct *work)
 {
 	struct agnx_priv *priv = container_of(work, struct agnx_priv,
@@ -233,12 +233,12 @@ static void agnx_periodic_work_handler(s
 
 //	spin_unlock_irqrestore(&priv->lock, flags);
 }
-
+#endif
 
 static int agnx_start(struct ieee80211_hw *dev)
 {
 	struct agnx_priv *priv = dev->priv;
-	unsigned long delay;
+	/* unsigned long delay; */
 	int err = 0;
 	AGNX_TRACE;
 
--- a/drivers/staging/agnx/phy.c
+++ b/drivers/staging/agnx/phy.c
@@ -723,6 +723,7 @@ static inline void set_adhoc_mode(struct
 	agnx_write32(ctl, AGNX_SYSITF_SYSMODE, 0x0);
 }
 
+#if 0
 static void unknow_register_write(struct agnx_priv *priv)
 {
 	void __iomem *ctl = priv->ctl;
@@ -752,6 +753,7 @@ static void unknow_register_write(struct
 	agnx_write32(ctl, AGNX_UNKNOWN_BASE + 0x58, 0x1F);
 	agnx_write32(ctl, AGNX_UNKNOWN_BASE + 0x5c, 0x1a);
 }
+#endif
 
 static void card_interface_init(struct agnx_priv *priv)
 {

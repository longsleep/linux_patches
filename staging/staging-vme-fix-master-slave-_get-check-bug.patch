From cota@braap.org  Mon Aug 10 11:36:27 2009
From: "Emilio G. Cota" <cota@braap.org>
Date: Mon, 10 Aug 2009 16:52:42 +0200
Subject: Staging: vme: fix {master,slave}_get check bug
To: Greg K-H <gregkh@suse.de>
Cc: linux-kernel@vger.kernel.org, devel@linuxdriverproject.org, Martyn Welch <martyn.welch@gefanuc.com>
Message-ID: <20090810145242.GA22320@braap.org>
Content-Disposition: inline


It's checking the wrong functions; fix it.

Signed-off-by: Emilio G. Cota <cota@braap.org>
Acked-by: Martyn Welch <martyn.welch@gefanuc.com>
Signed-off-by: Greg Kroah-Hartman <gregkh@suse.de>

---
 drivers/staging/vme/vme.c |    4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

--- a/drivers/staging/vme/vme.c
+++ b/drivers/staging/vme/vme.c
@@ -338,7 +338,7 @@ int vme_slave_get (struct vme_resource *
 
 	image = list_entry(resource->entry, struct vme_slave_resource, list);
 
-	if (bridge->slave_set == NULL) {
+	if (bridge->slave_get == NULL) {
 		printk("vme_slave_get not supported\n");
 		return -EINVAL;
 	}
@@ -499,7 +499,7 @@ int vme_master_get (struct vme_resource 
 
 	image = list_entry(resource->entry, struct vme_master_resource, list);
 
-	if (bridge->master_set == NULL) {
+	if (bridge->master_get == NULL) {
 		printk("vme_master_set not supported\n");
 		return -EINVAL;
 	}

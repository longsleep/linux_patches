From d072c5356bfd32f36b09883837b13128f9ec952d Mon Sep 17 00:00:00 2001
From: Greg Kroah-Hartman <gregkh@suse.de>
Date: Fri, 27 Feb 2009 22:41:54 -0800
Subject: Staging: line6: fix checkpatch errors in audio.c

Lots of warnings also fixed up.

Cc: Markus Grabner <grabner@icg.tugraz.at>
Cc: Mariusz Kozlowski <m.kozlowski@tuxland.pl>
Signed-off-by: Greg Kroah-Hartman <gregkh@suse.de>
---
 drivers/staging/line6/audio.c |    7 ++++---
 1 file changed, 4 insertions(+), 3 deletions(-)

--- a/drivers/staging/line6/audio.c
+++ b/drivers/staging/line6/audio.c
@@ -25,12 +25,12 @@ static char *line6_id[SNDRV_CARDS] = SND
 */
 int line6_init_audio(struct usb_line6 *line6)
 {
-	static int dev = 0;
+	static int dev;
 	struct snd_card *card;
 
 	card = snd_card_new(line6_index[dev], line6_id[dev], THIS_MODULE, 0);
 
-	if(card == NULL)
+	if (card == NULL)
 		return -ENOMEM;
 
 	line6->card = card;
@@ -49,7 +49,8 @@ int line6_register_audio(struct usb_line
 {
 	int err;
 
-	if((err = snd_card_register(line6->card)) < 0)
+	err = snd_card_register(line6->card);
+	if (err < 0)
 		return err;
 
 	return 0;

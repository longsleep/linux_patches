From linux-usb-devel-bounces@lists.sourceforge.net Wed Nov  8 06:38:13 2006
From: Mariusz Kozlowski <m.kozlowski@tuxland.pl>
To: Andrew Morton <akpm@osdl.org>
Date: Wed, 8 Nov 2006 15:37:04 +0100
MIME-Version: 1.0
Content-Disposition: inline
Message-Id: <200611081537.05283.m.kozlowski@tuxland.pl>
Cc: Greg KH <greg@kroah.com>, linux-kernel@vger.kernel.org, linux-usb-devel@lists.sourceforge.net
Subject: usb: usbmixer free kill urb cleanup
Content-Type: text/plain; charset="us-ascii"

- usb_free_urb() cleanup
- usb_kill_urb() cleanup

Signed-off-by: Mariusz Kozlowski <m.kozlowski@tuxland.pl>
Signed-off-by: Greg Kroah-Hartman <gregkh@suse.de>

---
 sound/usb/usbmixer.c |    9 +++------
 1 file changed, 3 insertions(+), 6 deletions(-)

--- gregkh-2.6.orig/sound/usb/usbmixer.c
+++ gregkh-2.6/sound/usb/usbmixer.c
@@ -1620,8 +1620,7 @@ static void snd_usb_mixer_free(struct us
 		kfree(mixer->urb->transfer_buffer);
 		usb_free_urb(mixer->urb);
 	}
-	if (mixer->rc_urb)
-		usb_free_urb(mixer->rc_urb);
+	usb_free_urb(mixer->rc_urb);
 	kfree(mixer->rc_setup_packet);
 	kfree(mixer);
 }
@@ -2056,8 +2055,6 @@ void snd_usb_mixer_disconnect(struct lis
 	struct usb_mixer_interface *mixer;
 	
 	mixer = list_entry(p, struct usb_mixer_interface, list);
-	if (mixer->urb)
-		usb_kill_urb(mixer->urb);
-	if (mixer->rc_urb)
-		usb_kill_urb(mixer->rc_urb);
+	usb_kill_urb(mixer->urb);
+	usb_kill_urb(mixer->rc_urb);
 }
